<footer class="clear-both p-4 mt-8 bg-light">
	<div>
		{{ sidebar_footer }}
	</div>
	<div class="mt-8 text-xs text-center">
		<div id="copyright">
			©{{ 'now'|date('Y') }}
			{{ options.copyright_text ? options.copyright_text : site.name }}
		</div>
		{% if options.footer_text %}
			<div id="site-info" class="text-navy-500">
				{{ options.footer_text }}
			</div>
		{% endif %}
	</div>
</footer>

{{ function('wp_footer') }}
